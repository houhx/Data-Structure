#pragma once
#include <iostream>
#include <stdio.h> 
#include <stdlib.h>
#include <string.h>
#include <algorithm>
using namespace std;

typedef int elemtype;

enum return_code{successful, out_of_memory,underflow, overflow, Range_error, Error};
